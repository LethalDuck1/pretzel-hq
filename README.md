## Admin login (custom)
Set these environment variables in Netlify:
- ADMIN_USER (ex: admin)
- ADMIN_PASSWORD (choose a strong password)

Then /admin will show a login screen. It also still supports the old admin-key unlock.
